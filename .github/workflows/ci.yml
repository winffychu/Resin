name: CI

# 独立验证流水线：在 release.yml 之外验证 go.mod/go.sum 与前端构建、
# 跑 go test —— 不影响 tag 触发的 publish/docker 流。
#
# 建立背景：fork 内某次 sing-box 升级到 v1.13.15 只改了 go.mod，
# go.sum 仍只有 v1.12.21 的 h1 条目，release.yml 的 `go build` 因此
# 报 `missing go.sum entry` 失败。本 workflow 在 CI 上跑 `go mod tidy`
# 把缺失的哈希补出来，并将生成的 go.mod / go.sum 作为 artifact
# 上传以便下载回仓库，避免本地依赖 Go 工具链。验证通过后，可
# 决定是否把同样改动并入 release.yml。

on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-test:
    name: Go (tidy + build + test)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build WebUI (embed.go 需要 dist/)
        working-directory: ./webui
        run: |
          npm ci
          npm run build

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: go mod tidy (补齐 go.sum 中缺失的 h1 条目)
        run: |
          go mod tidy -v
          if git diff --quiet -- go.mod go.sum; then
            echo "::notice::go.mod/go.sum unchanged after tidy"
          else
            echo "::notice::go.mod/go.sum changed after tidy (will be uploaded as artifact)"
            git --no-pager diff --stat -- go.mod go.sum
          fi

      - name: Build Go Binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          OUTPUT_NAME=resin-${GOOS}-${GOARCH}
          if [ "$GOOS" = "windows" ]; then OUTPUT_NAME="${OUTPUT_NAME}.exe"; fi
          mkdir -p build
          go build -trimpath -tags "with_quic with_wireguard with_grpc with_utls with_embedded_tor with_naive_outbound with_purego with_gvisor" \
            -o build/${OUTPUT_NAME} ./cmd/resin
          ls -la build

      # 仅在 linux/amd64 跑 go test —— 其它平台交叉编译跑测试不实际，
      # 且 secure_dns 等适配代码通过 GOOS=linux GOARCH=amd64 即可覆盖。
      - name: go test
        if: matrix.goos == 'linux' && matrix.goarch == 'amd64'
        env:
          CGO_ENABLED: 0
        run: go test ./...

      - name: Upload generated go.mod / go.sum
        if: matrix.goos == 'linux' && matrix.goarch == 'amd64'
        uses: actions/upload-artifact@v4
        with:
          name: go-lock
          path: |
            go.mod
            go.sum
          retention-days: 14

      - name: Upload built binary (smoke)
        uses: actions/upload-artifact@v4
        with:
          name: resin-${{ matrix.goos }}-${{ matrix.goarch }}
          path: build/resin-${{ matrix.goos }}-${{ matrix.goarch }}*
          if-no-files-found: warn
          retention-days: 7
